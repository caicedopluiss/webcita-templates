import { SampleTemplateContent, SampleTemplateTheme } from '../types';
interface Props {
    content?: SampleTemplateContent;
    theme?: SampleTemplateTheme;
}
export default function Landing({ content, theme }: Props): import("react/jsx-runtime").JSX.Element;
export {};
